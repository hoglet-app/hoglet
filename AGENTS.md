## Primary Reference

When working on PostHog API-related code, documentation, or behavior, use the official PostHog API docs as the primary source of truth:

- https://posthog.com/docs/api

If other sources conflict, prefer the official docs unless the user explicitly requests otherwise.

## Quality Check

After making code changes, run `flutter analyze` and address any findings (or report them clearly if they can't be resolved).
